[package]
name = "banto-storage"
description = "Banto storage: sqlx-based SQLite/PostgreSQL repository implementations (M2+)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
banto-core = { path = "../banto-core" }
