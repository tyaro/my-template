# セッション引き継ぎ（2026-07-22）— 採用性改善 & P0 修正の作業状態

対象読者: **この作業を別セッション/別PCで引き継ぐ AI エージェントまたは人間**。
コールドスタートから即座に続きを再開するための「作業の現在地」スナップショット。

> **最初にすること**: (1) この文書を読む → (2) 深掘りは下記「計画ドキュメント」を読む →
> (3) `git fetch origin` して下記ブランチ/PR の最新状態を確認 → (4)「次のアクション」から再開。
> リポジトリの恒久的な道案内は [AGENTS.md](../AGENTS.md)、不変条件は
> [conventions.md](conventions.md)。本書はそれらとは別の**セッション状態**の記録。

---

## 更新（2026-07-23）— コールドスタート差分

前回スナップショット（2026-07-22）から進んだ点。詳細は各節に反映済み:

- **P0 3 PR マージ済み**: #77 / #78 / #79 をすべて `main` に squash マージ（オーナー承認）。
- **AD-6 レイヤ① 実装・マージ済み**: `@banto/*` 全パッケージ（grid/forms/charts/dock/attachments/
  report）の可視文言を注入対応化（現行日本語デフォルト・byte-identical・後方互換）→ PR **#81** マージ済み。
- **AD-4 実施**: template-scope §7 の着手トリガに「外部採用者フィードバック」を追記 → PR **#82**（CI 緑・レビュー待ち）。
- **PR-C（Tauri CSP）着手**: `app.security.csp` を null → LAN 側（`security_headers.rs`）と対称な
  CSP に設定（差分は `connect-src` の Tauri IPC のみ）。**実機 Windows で `tauri build --debug` が
  成功**（サンドボックス制約は実機で解消）。現在 §3 チェックリストの**実機スモーク中**。
  ブランチ `claude/p0-tauri-csp`（`main` 分岐、未 push）。
  - 重要な設計判断: app.html のインライン初回描画スクリプトは、SvelteKit ブートストラップが
    `__sveltekit_<ランダム>` をビルド毎に含むため sha256 ハッシュのピン留めが破綻する。よって
    LAN 側で既に出荷済みの `script-src 'self' 'unsafe-inline'` を踏襲（ハッシュ化は将来のハードニング）。

---

## 0. この作業ストリームは何か（30秒サマリ）

「Banto テンプレートを採用者視点で評価してほしい」から始まり、以下へ発展した:

1. **採用者視点レビュー**（[adoption-review-2026-07.md](adoption-review-2026-07.md)）→
   計画（[adoption-plan-2026-07.md](adoption-plan-2026-07.md)、AD-1〜AD-6）。
2. **グローバル対応の方針決定**（外国人労働者増が実例）→ i18n を**レイヤ分割で段階導入**
   （[i18n-plan.md](i18n-plan.md)。当面はレイヤ①=パッケージ文言外部化のみ）。
3. **ChatGPT 計画レビューの統合**（[plan-review-integration-2026-07.md](plan-review-integration-2026-07.md)）
   — 指摘7件を実コードで全件検証（全 CONFIRMED）。最重要は `audit_config_get` の両経路
   ロール非対称（実バグ）。
4. **P0 実装**（3分割 PR）+ **AD-5**（受け入れCI）を実施 → 下記 PR。

**現在の局面**（2026-07-23 更新）: P0 3 PR（#77/#78/#79）と AD-6 レイヤ①（#81）は**マージ済み**。
AD-4（#82）は CI 緑でレビュー待ち。PR-C（Tauri CSP）は**実機 Windows でビルド成功し、実機スモーク中**
（上の「更新」節参照）。オーナー判断待ちの項目がいくつかある（§4）。

---

## 1. ブランチと PR の状態

| PR | ブランチ | 内容 | ローカル検証 | 状態 |
| --- | --- | --- | --- | --- |
| [#77](https://github.com/tyaro/banto/pull/77) | `claude/p0-audit-role-symmetry` | `audit_config_get` を両経路 Admin に統一 + rule 8 にロール床照合（CR-6） | 緑（verify:architecture / cargo test / fmt / prettier / eslint。ネガティブテストで赤確認） | **✅ マージ済み**（2026-07-23） |
| [#78](https://github.com/tyaro/banto/pull/78) | `claude/p0-doc-tool-consistency` | doc/tool 整合（チャート14種・scan-wedge記述・pnpm check説明・scan-wedge を tsc化）+ CR-7 バージョン整合検査 | 緑（check:versions / scan-wedge tsc / prettier / eslint） | **✅ マージ済み**（2026-07-23） |
| [#79](https://github.com/tyaro/banto/pull/79) | `claude/ad5-template-acceptance` | 受け入れCI（copy→rename→check）+ rename 統合テスト（AD-5） | 緑（`node --test scripts/rename.test.mjs` 3件） | **✅ マージ済み**（2026-07-23） |
| [#81](https://github.com/tyaro/banto/pull/81) | `claude/i18n-layer1-package-strings` | **AD-6 レイヤ①**: `@banto/*` 可視文言を `messages` 注入対応化（既定日本語・byte-identical） | 緑（verify:architecture 9/9 / pnpm check 0 errors / vitest grid176+forms27+charts210+dock146+attachments22+report60 / prettier） | **✅ マージ済み**（2026-07-23） |
| [#82](https://github.com/tyaro/banto/pull/82) | `claude/ad4-adoption-feedback-trigger` | **AD-4**: template-scope §7 着手トリガに「外部採用者フィードバック」追記 | 緑（verify:architecture / prettier） | **レビュー待ち**（CI 緑） |
| PR-C | `claude/p0-tauri-csp`（未 push） | Tauri CSP 設定（LAN と対称、§3） | 実機 `tauri build --debug` 成功。**実機スモーク中** | **実機スモーク中**（§3） |

- **計画ドキュメント**はこのブランチ `claude/tauri-template-evaluation-7koy5z` にのみ存在
  （main や PR ブランチには無い）。深い文脈が要るときはこのブランチを見る。
- P0 の各 PR は **`main` から分岐**した独立ブランチ（オーナー指示「PR を分ける」）。
- `src-tauri` のコンパイルは従来サンドボックス（Linux, webkit2gtk 不在）で不可だったが、
  **2026-07-23 に実機 Windows（WebView2 + cargo 1.91）で `tauri build --debug` が成功**。
  以後、実機がある環境では Tauri 側も実ビルド/スモークで担保できる（サンドボックスのみの
  環境では従来どおり静的検査（CR-6 等）+ `tauri-check.yml`）。

---

## 2. 計画ドキュメント（読む順）

すべて `docs/`（トラックA=保守者向け）、このブランチ上:

1. [plan-review-integration-2026-07.md](plan-review-integration-2026-07.md) — **最新の一次情報**。
   ChatGPT レビューの検証結果・P0 の3分割・CR-6/CR-7 設計・AD 更新・実施順序。
2. [adoption-review-2026-07.md](adoption-review-2026-07.md) — 採用者視点の評価（★評価・障壁）。
3. [adoption-plan-2026-07.md](adoption-plan-2026-07.md) — AD-1〜AD-6 + P0。§1 に既存計画との照合表。
4. [i18n-plan.md](i18n-plan.md) — グローバル対応の設計。レイヤ①/②/③分割、①の棚卸しと実装方針。
5. 既存計画に追記済み: [improvement-plan-2026-07.md](improvement-plan-2026-07.md)（続きポインタ）、
   [maintainability-review-2026-07.md](maintainability-review-2026-07.md)（CR-6/CR-7 追加）、
   [template-scope.md](template-scope.md) §4.3/§5（i18n 方針転換）。

---

## 3. 保留中: PR-C（Tauri CSP）— 再開に必要な材料

`apps/admin-template/src-tauri/tauri.conf.json` の `security.csp` が現在 `null`。設定すべきだが、
**厳格すぎる CSP はアプリを静かに壊す**ため、実機ビルド+スモークでの反復が必須
（オーナーが受け入れ条件に「Tauri 実動スモーク」を指定）。このサンドボックスでは
`src-tauri` をビルド/実行できないため、値を確定できない。

**監査で判明した CSP 上の制約**:

- `app.html` に**インライン `<script>`**（テーマちらつき防止 IIFE）→ `script-src` に hash か
  `'unsafe-inline'` が必要（Tauri の nonce 自動付与は Tauri 注入スクリプトのみで著者インラインは対象外）。
- Svelte のランタイム `<style>` 注入 → `style-src 'unsafe-inline'` がほぼ必須。
- 添付/バックアップ/CSV/チャート出力の `URL.createObjectURL`（blob:）→ `img-src blob:` 等。
- ログイン背景の `data:image/svg+xml` → `img-src data:`。
- Tauri `invoke()` → `connect-src 'self' ipc: http://ipc.localhost`。

**提案する叩き台 CSP**（実機で締め上げる前提）:

```
default-src 'self';
script-src 'self' 'sha256-<theme-script のハッシュ>';   // 暫定は 'unsafe-inline'。hash 値は実ビルド出力で確定
style-src 'self' 'unsafe-inline';
img-src 'self' data: blob:;
font-src 'self';
connect-src 'self' ipc: http://ipc.localhost;
object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'self'
```

補足: tauri.conf の CSP は**デスクトップ Webview のみ**に効く（LAN ブラウザ配信側は
`banto-server` のセキュリティヘッダが別途担う）。インラインスクリプトの hash 化は
SvelteKit `kit.csp`（hash モードで app.html も自動ハッシュ）に寄せる案も有力 — これも実機確認要。

**実動スモーク・チェックリスト（PR-C の受け入れ条件）**: CSP 適用後、実機で ① ログイン/初回
セットアップ ② `invoke()`（items 一覧・作成）③ テーマ切替のちらつき無し ④ 添付の画像
プレビュー/サムネイル（blob:）⑤ CSV/バックアップのダウンロード ⑥ 設定画面の QR 表示
⑦ Console に CSP 違反ゼロ、が壊れないこと。

**再開手順**: 実機で `claude/p0-tauri-csp` を `main` から作成 → 叩き台を適用 → チェックリストを回して
値を確定 → PR 作成。

---

## 4. オーナー判断待ち（再開前/最中に決める）

1. **PR-C の CSP 値** — 実機スモークで確定（§3）。
2. **既存 `v0.1.1` タグの是正** — マニフェストは `0.1.0`。bump（0.1.1 へ）か誤タグ削除か。
   リリース工程（AD-3）の判断。CR-7 の通常CIはマニフェスト間一致を見るので現状は緑、
   タグCIモードは `v0.1.1` で赤（＝ドリフト検出済み）。
3. **AD-1 ポジショニング宣言の文言** — 「対象読者/非対象」。多言語方針（段階導入）を反映。
4. **AD-2 視覚訴求** — README に載せる既存スナップショット（`e2e/visual/**`）の選定。
5. **SECURITY.md / CONTRIBUTING** — 連絡先メール公開の可否・外部PRを受けるかの方針。
6. **AD-6 レイヤ①の着手可否** — パッケージに `messages` props を追加する公開API変更
   （既定は日本語のままで見た目不変）。設計は i18n-plan §3.2 に確定済み。

## 5. 次のアクション（優先順・再開時のメニュー）

**A. レビュー/マージ系（オーナー）**: ~~PR #77 → #78 → #79 を確認しマージ~~ → **✅ 2026-07-23 完了**
（3件とも `main` にマージ済み）。

**B. 外から進められる実装（別セッションで着手可・実機不要）**:
- ~~**AD-6 レイヤ①**: パッケージ可視文言の外部化~~ → **✅ 完了（PR #81 マージ済み）**。全 `@banto/*`
  パッケージに `messages` 注入を実装（既定日本語・byte-identical）。
- ~~**AD-4**（極小）: template-scope §7 の着手トリガに「外部採用者フィードバック」を追記~~ →
  **✅ 完了（PR #82 レビュー待ち）**。

**C. 実機/権限が必要（オーナーのPC・GitHub Web）**:
- **PR-C**（§3）→ **着手中**。実機 Windows でビルド成功、`claude/p0-tauri-csp` にコミット済み、
  §3 チェックリストの**実機スモーク中**（スモーク合格後に push + PR）。
- **GitHub 設定**（description/topics/Template repo 化/OG画像 — Web UI 必須。MCP ツールでは設定不可）、
  **リリース工程**（AD-3。Release 作成 + マニフェスト/タグ整合）は未着手。

**D. 小さな判断が要る doc（オーナー入力後に着手）**: AD-1 / AD-2 / SECURITY・CONTRIBUTING（§4）。

## 6. 環境と検証コマンド（このリポジトリの制約）

- **`src-tauri` はこのサンドボックスでコンパイル不可**（webkit2gtk 不在）。Tauri コマンド側の
  変更はコードレビュー + 静的検査（`verify:architecture` rule 8 / CR-6）+ `tauri-check.yml`
  （週次/パス条件付き）で担保する。
- 検証: `pnpm check`（型）/ `pnpm verify:architecture`（9ルール+CR-6）/ `pnpm check:versions`
  （CR-7、PR-B ブランチ以降）/ `cargo test -p admin-template-core -p banto-server ...` /
  `node --test scripts/rename.test.mjs`（AD-5 ブランチ）/ `prettier --check` / `eslint`。
- **依存を足さない**（conventions §3）: 追加スクリプト（verify-architecture / check-versions /
  rename.test）はすべて Node 標準のみ。
- ブランチ規律: 開発は指定ブランチで、他ブランチへの push は明示許可が要る運用だった
  （オーナーが「PR を分ける」と指示済み。P0 は `claude/p0-*`）。

## 7. この作業で確定した主要な決定（覆さないための記録）

- グローバル対応は**フルではなく段階導入**。テンプレ側は当面**レイヤ①のみ**（i18n-plan §1）。
- P0 の `audit_config_get` は **Tauri を Admin に揃える**（REST が仕様＝正、オーナー決定）。
- 機械検査は CR-6（ロール床）/ CR-7（バージョン）で限定再開。それ以上は実需ドリブン。
- 配布（署名/リリース）は「完成」ではなく**レシピ化 + 実需ドリブン**（テンプレは採用者の
  代わりに署名できない）。
- codegen（P4-9）より先に **AD-5 受け入れCI で手動導線の担保**を優先（実施済み）。
